import 'package:flutter/material.dart';

class AppColors {
  // --- Your Branding Colors ---
  static const Color primaryOrange = Color(0xFFF05D00);
  static const Color primaryCyan = Color(0xFF00E9EF); 

  // --- Study Flow Navy Theme (From Home/DeckCard) ---
  static const Color primaryNavy = Color(0xFF1A4594);    // Used for headers & buttons
  static const Color navyLight = Color(0xFFE8F0FF);      // Used for icon backgrounds
  static const Color navydark = Color(0xFF0C2B64);

  // --- Dark Mode Surface Colors ---
  static const Color background = Color(0xFF121212);    
  static const Color surface = Color(0xFF1E1E1E);       
  static const Color surfaceLight = Color(0xFF2C2C2C);  
  static const Color scaffoldBg = Color(0xFFF8F9FB);     // The light gray background

  // --- Status Colors ---
  static const Color success = Color(0xFF4CAF50);       
  static const Color error = Color(0xFFE53935);         
  static const Color starGold = Color(0xFFFFD700);      

  // --- Text Colors ---
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB3B3B3);
  static const Color textDark = Color(0xFF333333);       // Used for text on light backgrounds
}